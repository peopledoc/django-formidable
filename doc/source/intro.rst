Introduction
============

:mod:`django-formidable` provides the ``python/django`` side in order to create/edit custom django forms.
The main part is to provide an RESTful API which can be plug-in with a front-end application. (see :mod:`formidable-ui`).
You can have a look on provided endpoint in order to build forms through API.
